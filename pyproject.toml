[project]
name = "padel-time"
version = "0.1.0"
description = "Padel court availability aggregator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.128.0",
    "httpx>=0.27.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
    "cachetools>=5.3.0",
]
